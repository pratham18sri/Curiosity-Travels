"use client";import {FormEvent,useState} from "react";import {useRouter,useSearchParams} from "next/navigation";
export default function LoginPage(){const router=useRouter(),search=useSearchParams();const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const j=await r.json().catch(()=>({}));setLoading(false);if(!r.ok){setError(j.error||"Unable to sign in");return}router.push(search.get("next")||"/portal")}return <main className="login"><section className="loginHero"><div className="brand"><div className="logoMark">◈</div><div>CURIOSITY<small>TRAVEL</small></div></div><div className="heroCopy"><div className="eyebrow" style={{color:"#b7f4ee"}}>WORLD OF POSSIBILITIES</div><h1>Every great journey<br/>starts with curiosity.</h1><p>Your travel planning workspace.</p></div><div style={{position:"absolute",bottom:36}}>Curiosity Travel · 9909000642</div></section><section className="loginPanel"><form className="loginBox" onSubmit={submit}><div className="logoMark">◈</div><div className="eyebrow" style={{marginTop:28}}>CURIOSITY TRAVEL PORTAL</div><h2>Welcome back.</h2><p className="muted">Sign in to plan journeys and create quotations.</p>{error&&<div className="error">{error}</div>}<div className="field"><label>Login ID</label><input value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn" style={{width:"100%"}} disabled={loading}>{loading?"Signing in...":"Sign in →"}</button><p className="muted" style={{fontSize:13}}>Staff and agent accounts are created by Curiosity Travel.</p><p>☎ 9909000642</p></form></section></main>}