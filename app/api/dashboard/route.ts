import {NextResponse} from "next/server";import {connectDB} from "@/lib/db";import {Catalog,Quote} from "@/lib/models";import {getSession} from "@/lib/auth";export async function GET(){const u=await getSession();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});await connectDB();const owner=u.role==="admin"?{}:{ownerId:u.id};const [quotes,hotels,activities,cabs]=await Promise.all([Quote.countDocuments(owner),Catalog.countDocuments({type:"hotels",active:true}),Catalog.countDocuments({type:"activities",active:true}),Catalog.countDocuments({type:"daily-cab-rates",active:true})]);return NextResponse.json({quotes,hotels,activities,cabs})}